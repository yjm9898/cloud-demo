server:
  port: 80
spring:
  application:
    name: cloud-provider-hystrix-order80

eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka

# feign hystrix 启用
feign:
  hystrix:
    enabled: true
